[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "buddy-flash-app"
version = "0.1.0"
description = "Runpod Flash endpoints for Buddy accessibility analysis"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = ["runpod-flash"]
